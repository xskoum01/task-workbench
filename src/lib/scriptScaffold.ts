/**
 * Minimal script scaffold for new script files created by Task Workbench.
 *
 * Rules:
 *   - No TODO comments — AI Kit rules forbid stub/placeholder comments.
 *   - No IIFE, namespace, class, or module wrapper — preserves customer style.
 *   - Minimal: a single header comment so the file is valid and non-empty.
 */

export function buildScriptScaffold(): string {
  return '/**\n * Script file created by Task Workbench.\n */\n';
}
